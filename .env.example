# Copy to .env.local and fill in for staging/production. Local dev
# defaults already exist in .env.local — this file documents the shape
# for whoever configures a Vercel deployment.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_API_URL=
